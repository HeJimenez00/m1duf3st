---
const { img, href, size = 'xl' } = Astro.props
---

<article class={`flex items-stretch h-full ${img}`}>
	<a class={`group p-8 bg-zinc-400/5 border border-white/10 rounded-xl hover:border-[#FCD91C] hover:bg-white/10 transition w-full ${size === 'xl' ? '' : 'h-27'} flex items-center justify-center`} href={href} target='_blank' rel='noopener noreferrer nofollow'>
		<img class='group-hover:grayscale-0 group-hover:scale-110 group grayscale transition-all w-auto object-contain' src={`./patrocinadores/${img}.svg`} alt={`Logo de la empresa ${img}`} />
	</a>
</article>

<style>
  .autentia {
    grid-column: 1 / 3;
    grid-row: 1 / 3;
  }
  .nttdata {
    grid-column: 3 / 5;
    grid-row: 1 / 2;
  }
  .github {
    grid-column: 6 / 7;
    grid-row: 1 / 3;
  }
  .github img {
    height: 100%;
  }
  .warp {
    grid-column: 4 / 6;
    grid-row: 2 / 3;
  }
</style>
